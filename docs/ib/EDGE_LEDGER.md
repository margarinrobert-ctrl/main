
| 2026-08-30 | Reverse-engineer why two specific longs did not fire on a 1-minute US30 chart | **diagnosed, not a strategy failure** | the researched w=20 on 30m bars is 600 MINUTES; on a 1m chart it is 20, and the two exhaustion events were 320 and 54 minutes back | three blockers: a unit error (bars vs minutes), `>` failing on an exact tick equality, and the user's own EMA override; fixed by declaring the order-flow settings in MINUTES and letting a touch count as a break (measurably free: research p 0.000 -> 0.001) | audit every shipped script for settings that are bar counts rather than durations |
| 2026-08-31 | The Initial Balance retracement family, 777,600 configurations x 2 markets x 2 passes, with ADX / IB-range / close-position / EMA-cross conditions | **family rejected; one cluster carries one clean read** | as published it scores PF 0.83/0.78 US30, 0.92/0.85 US100, 0.940 NQ and LOSES to a risk-matched random entry at p 0.76-0.99; every marginal is negative at every setting of every axis; 2/25 research leaders positive on locked against 6-12 by chance and 0/25 clear their control | first ranking was two artifacts at once -- the risk is a DIFFERENCE of two swept fractions so R rewarded an 11.6-point stop at 10:1, and the fill bar was exempt from its own stop (worth up to +0.09 ATR/trade to 49.5% of the grid); re-scored in ATR units with the fill bar tested, the profitable share fell 21.5% -> 13.5%. `EMA 13 under 48` was in 74% of the top 1000 and fails on both US100 marginal columns; only the ADX floor beats `off` in all four. One cluster read once on NQ, which chose nothing: +0.354 ATR/trade, PF 1.80, control p 0.003, **n 48** | vectorbt agreed on the trade count 100.0% in all four cells -- first match on this branch after three failed transcriptions; a UTC/New-York clock slip on `NQ_1m` nearly produced a false table |
| 2026-08-31 | EMA 16/64 trend following with a hard four-hour ceiling, ADX and ATR, 243,000 configurations x 2 markets | **rejected on three markets** | as briefed PF 0.948/0.849 US30, 0.898/0.947 US100, 1.004 NQ, losing to a minute-of-day matched random entry with the SAME management at p 0.53-0.99; the two research survivors both went negative on US30's locked block and one repeated that failure on NQ | `corr(research, locked)` over 175,000 scorable configs is **-0.0698 on US30**, +0.0937 on US100 -- a research ranking carries no information here. Only the ATR floor beats `off` in all four columns; `ADX <= 20` won 55% of the top 1000 and splits across markets on the marginal read, the second study running where the consensus favourite fails that test. The four-hour ceiling spans 0.02 ATR/trade from 1h to 4h -- free and worthless | the matched control was broken first: sampled bars inherited the position lock without inheriting chronological ORDER, so its spread exploded and p sat at 0.40 for every row; sorting them moved 0/25 to 6/25. vectorbt agreed on trade count 99.4-99.9% and put the gross edge below the round turn |
| 2026-08-31 | Port `FTM_OPENING_RANGE_BREAKOUT_MNQ_v1_8_0_RC1` (2,872 lines of NinjaScript) to Pine 6 for TradingView backtesting | **ported and verified to RUN; NT8 parity still unproven** | a Python transliteration of the shipped Pine over 1.05M real one-minute bars reaches every branch: 473 eligible sessions, 458 admitted signals, 82 geometry rejects, 31 touch vetoes, 3 prior-session and 7 kNN overrides, 79 RC1 direct actions, 374 model labels, 342 entries and all four exit reasons | the first port was silently DEAD -- lint clean, logic correct on inspection, and **0 trades** -- because `sameCashDate` was dropped as boilerplate. The 18:01 ET bar belongs to the next trading day and its closeMinuteEt of 1082 exceeds the 960 flatten, so the cash-close branch fired at the START of every session. Also caught: Pine functions may not assign to globals (lint cannot see it), and the exit bracket must be placed WITH the entry or the fill bar is unprotected -- V56's defect again | seven documented places where Pine cannot equal NinjaTrader are in the file header: inverted bar timestamps, no SessionIterator, no order handles, the closed-equity source, the back-adjustment table, and the k-NN runtime cap |
| 2026-08-31 | The FTM Pine port compiles and loads on TradingView but the Strategy Tester shows no trades | **not a port defect -- the warm-up cannot complete on a 1-minute chart** | the rule needs 120 completed sessions of opening-range history plus 21 session closes before `submitActual` can be true; an ETH 1-minute series is ~1,380 bars a day, so Basic's 5,000-bar budget is 2.6 sessions and Premium's 20,000 is 10.4. The warm-up never finishes on any plan | fixed by computing the warm-up context inside a 15-minute `request.security`, which has its own bar budget. The substitution is EXACT: the 09:30 fifteen-minute bar is the opening range, verified over 764 dates with zero high/low mismatches, and the 15:45 close matches the 16:00 one-minute close on 735 of 735 | the panel now names the blocking reason outright -- wrong timeframe, no 23:00 UTC bar (an RTH-only chart), warm-up incomplete, or none |
| 2026-08-31 | Backtest `FTM_OPENING_RANGE_BREAKOUT_MNQ_v1_8_0_RC1` on MNQ | **clears its matched control -- with three qualifications that outrank the headline** | 342 trades, +$11,661 on $50,000, PF 1.351, win 47.4%, +0.1620 R, max DD -$3,032, ret/DD 3.85, Sharpe 1.46, bootstrap P(mean<=0) 0.005; excess **+0.1013 R over a random quarter-hour entry with identical geometry and exits at p 0.004** | the top 5% of trades are **117% of net**; 2023 is flat at -$214 and only 2024 is strong; and the **conditional 15:30 exit alone contributes more than the entire net** (+$14,687). Tagged by decision path, the plain no-refinement path earns +0.20 R on 211 trades against the **RC1 direct action's +0.10 on 62** -- 1.8.0's headline behaviour dilutes rather than improves. The 15-NN direction model fires 7 times in 458 admitted signals | ConfidenceScaledPercent makes the most dollars and is the worst risk-adjusted mode (PF 1.275, drawdown 2.6x FixedDollar) because it drops the defensive one-contract cap where the drawdown comes from. NQ price path with MNQ specs; levels are synthetic so basis-point features are distorted; not an NT8 Analyzer run |
| 2026-08-31 | The FTM Pine port compiles but the TradingView Strategy Tester stays empty, twice | **the 120-session warm-up is the binding constraint and it is worth nothing** | varying ONLY the opening-range lookback over 1.05M bars: 120 -> 342 trades PF 1.351 +0.1620 R; 90 -> 371 PF 1.421 +0.1905; 60 -> 399 +0.1607; **40 -> 417 PF 1.375 +0.1600**; 20 -> 434 +0.1476. Forty sessions is ~3,700 fifteen-minute bars, loads on any plan, and costs 0.002 R a trade | every fail-closed gate is now counted separately (contiguity gap, missing 23:00 UTC reference open, incomplete opening range, calendar exclusion) and the panel names the binding one; opt-in deviations added for the reference-open fallback and strict minute contiguity, both labelled | a blocked date and a date with no signal are indistinguishable from outside a fail-closed system -- instrument every gate the first time, not the third |
| 2026-08-31 | The FTM Pine port still shows an empty TradingView Strategy Tester on the third round | **four fail-closed gates, not one -- all now switchable, counted, and defaulted for TradingView** | the gates are the 120-session warm-up refusal, the lookback itself, MINUTE CONTIGUITY (which hard-fails in three separate places) and the 23:00 UTC reference open. Contiguity matters because NinjaTrader's feed carries a bar for every minute and TradingView omits minutes with no trades, so the source's fail-closed rule kills dates on this feed that it would have traded on that one | measured cost of each over 1.05M bars: warm gate OFF 342 -> 454 trades, +0.1620 -> +0.1608 R, PF 1.351 -> 1.374; lookback 120 -> 40 gives 417 trades at +0.1600; contiguity OFF gives an identical 342 trades at +0.1583; all three together 454 trades at +0.1434 and PF 1.322. Defaults now ship TradingView-safe and the source's exact values are one tick-box each | debugging a fail-closed system one gate per round is how three rounds get spent -- enumerate every path that can silently return zero FIRST, then make each switchable and counted |
| 2026-08-31 | Complete quant test on the V41 EMA-cross/Donchian stack with the AROON OSCILLATOR added: correlation matrices, out-of-sample, Monte Carlo, robustness, 1,166,400 configurations, vectorbt | **Aroon rejected as an identity, not as a weak signal; the family underneath decays to zero** | where the Aroon period is no longer than the Donchian entry length the breakout bar IS the N-bar high, so `Aroon Up = 100` and `osc >= 0` hold on **100.0% of breakout bars -- 60,000 bars, three markets, zero exceptions**. `osc>=0` and `up>=70` remove not one signal; the only rung that binds (`osc>=50`) costs 10-20 $/trade on every locked block. Aroon correlates **+0.57 to +0.60 with the EMA state it was being added to** | the Aroon axis distributes 20-29% across its four inert settings in the top 1000 -- the signature of an axis the ranking cannot see. Marginals: no Aroon setting beats `off` in more than 2 of 6 market-block columns; NO TAKE PROFIT wins 5 of 6 (fifth confirmation on this branch); ADX>=20, which the leading cell uses, beats `off` in only **2 of 6** while CHOP<=45 manages 4. `corr(research, locked)` is **-0.4426 on NQ** over 121,282 configurations. The one-rung box is **128/128 profitable on research on all three markets and 26.6% on NQ locked** -- a perfect plateau that does not replicate -- and 5/5 in-block walk-forward folds are positive on all three | an independent vectorbt build agreed on the SIGNAL SET at 99.6-99.9% (every disagreement inside the EMA(62) warm-up) and on the trade count at 97.6-100%, then reported 4-22 fewer points a trade. **I first attributed that to the channel exit's fill convention and that was wrong by an order of magnitude** -- `mean(open[j+1]-close[j])` over those exits is -0.21 to +0.55 points, not always signed the same way. The gap is vectorbt's own execution: its STOP exits land on the SAME bar and price 9-110 points worse, and 10-18% of its exits do not fill at the `price=` series. The arbiter is `v60_parity.py`, the shipped Pine's own order model written out: 99.5-100% of the trade count and -2.6% to +4.6% of the points, negative on every locked block. Three vectorbt traps recorded: `sl_stop` anchors to the bar's CLOSE not the `price=` fill; an unshifted exit signal with `price=open` is lookahead worth +22 to +100 points a trade; `stop_exit_price` does not fully control its exit fills |
| 2026-08-31 | Make the Aroon oscillator actually do something in the shipped V60 Pine, and add an entry window and a hard flatten | **one real defect fixed, one identity confirmed from the other side, both new features priced** | the preset resolution overwrote `aroonMode` with `off`, so the input was DEAD unless the preset dropdown read Custom -- a preset must set what it has an opinion about and nothing else. Beyond that the input was working: the Aroon simply cannot refuse a breakout it is read on. Reading it ONE BAR EARLIER is the only version that can, because the bar before a breakout need not be the N-bar high | on NQ 60m at Donchian 55, `osc>=0` / `up>=70` / `osc>=-50` at the SIGNAL bar return **54 trades, +67.30 research / -20.02 locked -- identical to the cent to `off`**; at the PRIOR bar 52 / 45 / 53 trades and +74.52 / +98.19 / +69.42. The prior-bar read beats the signal-bar read on research in every pair and is mixed on locked, on 30-50 locked trades and ONE market, against a whole-grid marginal saying no Aroon setting beats `off` in more than 2 of 6 columns. Ships as a knob with its numbers, not as a result | the flatten costs **57% of the per-trade result at 16:00 and all of it at 12:00** and RAISES the trade count 83 -> 103, which is where the cost goes; the best research window (09:30-12:00, +73.37) reads **-12.53** on locked -- tenth confirmation of the intraday finding. "Flat by 16:00" means flat at the 16:00 OPEN, so the order goes in on the bar before, and a signal whose fill would land past the cutoff is REFUSED rather than opened and closed at the same price. Parity on all five configurations: **trade counts 100.0%**, points -2.8% to +0.1% |
| 2026-09-01 | Add the MACD to the V60 breakout, and a session window / flatten / ADX floor to the YouTube Turtle | **MACD is the third indicator to be the breakout restated; all three Turtle gates are the wrong shape** | CONFIRMED ON THREE MARKETS: `macd > 0` at 12/26/9 passes **100.0% of US30's Donchian-55 breakout bars** (162 trades, +48.44 research / +20.28 locked, IDENTICAL to no filter), 99.9% of US100's and 99.8% of NQ's. Joins RSI>=55 at 94.7% and aroon osc>=0 at 100.0%. The only reading that binds is a FRESH cross (42-45% of signals) and it fails every shape test at once -- locked +9.48 NQ / +99.44 US100 / +0.94 US30 while making US100's RESEARCH worse (+26.61 -> +19.70), and flipping +9.48 -> **-29.51** one parameter rung away on NQ | **MA type IS a degree of freedom for the MACD**, unlike a single average: EMA vs SMA readings of `hist > 0` agree on only **79.2%** of bars against STUDY_MA_LAG's 89.5-97.3%, because a difference of two averages plus a third smoothing compounds the lag mismatch. And the AROON prior-bar reading, which looked mildly encouraging while only NQ was on disk, is the WRONG SHAPE once the feeds came back: at preset A it improves NQ on both blocks (+41.28 -> +55.86 / +6.81 -> +14.09) and makes RESEARCH WORSE on US100 (+35.22 -> +29.22) and US30 (+53.49 -> +42.48). The 16:00 flatten costs **57% / 69% / 61%** of the research result on NQ / US100 / US30 -- every market -- and no window beats all hours on research on more than one | on the YouTube Turtle (NQ 1H), a window, a flatten and an ADX floor ALL move in-sample DOWN and out-of-sample UP: flatten 12:00 takes IS +0.193 -> +0.121 and OOS **-0.090 -> +0.174**; ADX >=20/25/30 gives OOS +0.140 / +0.156 / **+0.307** as n falls 62 / 57 / 36. Wrong shape, on the ONE market of six where the frozen rules failed OOS, with a gradient that is what restrictiveness alone looks like, and a flatten that RAISES the trade count 158 -> 326. Opposite sign to the same flatten on V60 (-57%). The frozen kernel was COPIED and parity-asserted (158/158, 70/70, identical R), never parameterised |
| 2026-09-01 | Ship ONE Turtle with MACD, Aroon, an entry window, a flatten and an ADX floor, gates ON at the best-measured values | **the marginal consensus ships; the top row is a 40-trade artifact** | 960 cells, 685 scorable at 40+ in-sample trades, selected on the IN-SAMPLE block by MARGINAL AVERAGE PER AXIS and read once out of sample. Top row by in-sample R: n 40, **+0.682**, PF 4.00 -> OOS n 18, **-0.058**. Marginal consensus: n 64, +0.414, PF 2.84 -> OOS n 34, **+0.207**, PF 1.75 -- the only one of the three positive on both blocks, against the ungated frozen rules' +0.193 / -0.090 | marginal R/trade by axis: MACD `hist>0 and rising` **+0.390** vs off +0.184 (the strongest axis); Aroon `osc>=0` +0.304 vs off +0.222, with all three ON settings within 0.004 of each other because they are one indicator read three ways; window 08:00-12:00 +0.312; flatten none +0.349 (16:00 +0.247 is the best ON setting); **ADX off +0.314 beats every floor** and ships at >=20 only because it was asked for. Aroon length 25 against a 20-bar entry channel, so the Donchian identity does not apply | neighbourhood smooth rather than spiked -- one axis at a time, OOS reads all hours +0.239, flatten 12:00 +0.257, MACD hist>0 +0.124, ADX off +0.327. Caveats attached and not repeated at every switch: ONE market, and NQ is the one of six where these frozen rules failed OOS. Frozen kernel COPIED and parity-asserted (158/158, 70/70, identical R), never parameterised |
| 2026-09-01 | Reverse-engineer the Initial Balance model: which conditions create the edge | **the edge is the RETRACEMENT DEPTH, not the Initial Balance and not the conditions** | the retracement axis is monotone across all five rungs with its control p falling beside it: 0.00 (buy the break) **+0.0226 at p 1.000**, 0.10 +0.1072, 0.25 (as published) +0.2148 (p 0.075), 0.40 +0.2397, 0.50 **+0.3087 at p 0.000** on 329 trades. A retracement fraction of the IB range IS a resting limit priced in IB units -- sixth route to the `atme`/`STUDY_LIMIT_ENTRY` finding and the first where it was hiding inside someone else's indicator | **the four declared conditions buy +0.0457 ATR/trade and cost 85% of the sample** (geometry alone +0.3087 on 329 at p 0.000 vs the full rule's +0.3544 on 48). `ADX>=20` is BACKWARDS -- dropping it gives **+0.6489** and its ladder falls monotonically with ADX strength, with the inverted rung best at +1.9341 on 11 trades. Exactly one condition carries: the last IB bar's CLOSE POSITION in its range, alone +0.4132 on 222 trades, bottom-40% **-0.3211** against upper-half +0.3544. IB length barely matters (30/60/90 all p 0.000) | three killer tests: 275% of net comes from the 15:55 FLATTEN and -175% from the stops, so this is a held directional position and not a barrier system; the infinite stop scores +0.1546 against the shipped +0.3544 so the stop is NOT decoration (unlike M4), though its 48-trade neighbourhood has a negative rung at 0.80 that vanishes on the 329-trade sample; and the 48 selected days travel **-0.5945 ATR** finishing up 47.9% against +0.1775 / 57.1% for all others, so it is NOT a drift picker. No target beat every target for the sixth time. **NQ IS SPENT** -- one pre-registered read plus ~60 anatomy cells, so its p-values are descriptive now |
| 2026-09-01 | Test an uploaded CMMA daily mean-reversion notebook on MNQ -- does it have an edge | **no look-ahead, no demonstrable edge, and costs are not the obstacle** | the audit and execution-alignment check are CLEAN and the signal at date D uses data through calendar day D-2, so this is an effect too small to demonstrate rather than a leak. US100 nine years, 2,262 traded days: **Sharpe +0.39 +- 0.33 net**, deflated Sharpe **0.057** against 34 trials whose expected best-of-noise is **0.85**, PBO 0.35, block bootstrap P(Sharpe<0.5) **67.5%**, three of nine years negative, best 1% of days **240% of net**. NQ three years: +0.70 +- 0.58, deflated 0.150, shift null p 0.078 | three inflators, in order of size: `pnl = signal*(close-open)` SUMMED OVER INTRADAY BARS drops every between-bar gap (+13% NQ, +24% US100); `sr = eq.mean()/eq.std()` on a CUMSUM IS NOT A SHARPE (prints 0.875 where the true annualised figure is 0.83 -- close by luck, and it hides a +-0.58 SE); and zero costs, which is the SMALLEST because the EMA smoothing holds turnover at 0.04 contracts/day and breakeven is 24-29 bps against 0.9-2.6 charged | **THE COMPONENT ATTRIBUTION INVERTS BETWEEN FEEDS**: on NQ the KER weighting is the whole strategy (+0.76 with, +0.16 without) and bare `sign(cmma)` earns -0.01; on US100 bare `sign(cmma)` is the BEST row at +0.46 and every layer makes it worse. `tanh` and the EMA smoothing never help on either feed. One encouraging fact: the shift null passes on the long sample (p 0.045) and the holdout held on both. Also caught: `metrics.sharpe_standard_error` takes a PER-PERIOD Sharpe -- feeding it an annualised one understates the error bar by sqrt(252) |
| 2026-09-01 | Make the CMMA strategy perform better, and clean up its TradingView rendering | **one candidate survives a two-feed gate, and it is a removal** | seven pre-declared candidates scored IN-SAMPLE on NQ and US100, surviving only by beating the notebook on BOTH (the attribution inverts between those feeds, so a one-feed choice is the trap): drop tanh + drop the EMA smoothing is the only clean survivor -- NQ +0.76 -> +0.83, US100 +0.22 -> +0.47 in-sample; holdout read once: NQ +0.73 -> **+1.99** (PF 1.28 -> 1.83), US100 +0.65 -> **+0.96** (PF 1.23 -> 1.32). A dead band also passed the gate but barely (US100 +0.02). Both removed components had already measured as inert | deflated Sharpe of the survivor **0.16** against 40 trials, so still not distinguishable from the best of 40 random tries; holdout BETTER than in-sample on both feeds, the regime warning; without tanh the signal is unbounded (mean 0.076 -> 0.137, p99 0.75, max 1.55) so the Pine's 100-contract cap is load-bearing though it never binds at a base of 50. **Starting at 09:30 instead of 08:00 HURT on both feeds** -- opposite to four prior 07:00-09:30-is-worst findings, because a held daily position wants the pre-open hour | the Pine was rewritten for TradingView: two orders a day (`strategy.entry` at the session's first open, `strategy.close_all` at its end, pyramiding 0) instead of delta-sizing with pyramiding 100; one pane on one scale (signal columns in (-1,1), position divided by the base size); a 4-row panel that flags a target of zero. Parity re-verified in both the shipped and the original configuration on both feeds |
| 2026-09-01 | Parameter stability, Monte Carlo on every cell, cluster analysis and walk-forward for the Zeta FX IBS New York session EA | **a drift exposure wearing a signal; no optimiser beats the author's defaults; nothing clears a matched control on a test block** | 2,352 cells on four feeds, bar-by-bar parity 9/9 identical; research grids 78-100% positive with a random-session control earning +0.09 to +0.14 R; default excess over control on the three test blocks -0.005 / +0.014 / -0.008 R (p 0.51 / 0.46 / 0.52); walk-forward chosen cells WFE 0.09-0.45 against the fixed default's 0.91 on US100; surface coherence 0.96-0.98; 117-137 clusters; the 0.5x stop the optimiser buys is the R denominator (+0.377 R at +15 pts vs +0.133 R at +47 pts) | `docs/ib/STUDY_IBS_SESSION.md` |
| 2026-09-01 | Backtest the published Double Donchian Channel Breakout Pine on US30, US100 and NQ instead of BTC | **US30 +22% on a two-rung island, US100 -44% and negative at zero cost, NQ flat; the script's partial TP re-fires every bar** | 1-hour bars, whole files, Pine order model reproduced literal and as-intended; trade-count-matched random-entry controls p 0.037 / 0.84 / 0.37; width-filter ladder 2/3/4/5% on US30 -22/+22/+17/-14%; timeframe ladder inverts; every control negative; header window holds 0/3/1 trades | `docs/ib/STUDY_DOUBLE_DONCHIAN.md` |
| 2026-09-01 | Test The Strat combo engine EA (bar-type reversal combos + location score, one-bar stop order, 2R) on US30, US100 and NQ | **negative on every market, block, side and 24/27 years; gross a coin; beats a random bar by less than the geometry costs** | 15m as configured -0.250 / -0.116 / -0.090 R over 1,315 / 1,459 / 489 trades, zero-cost 0.000 / +0.071 / -0.001; random-trigger control -0.379 / -0.256 / -0.145 (p 0.000 / 0.000 / 0.18); location filter passes 68-93% of triggers and removing it is no worse; buffer, point-scale, RR, combo-alone, timeframe ladders all sub-zero except one 37-trade cell | `docs/ib/STUDY_THE_STRAT.md` |
| 2026-09-01 | Test `FTM_OPENING_RANGE_BREAKOUT_MNQ_v1_8_0_ALPHA2` (NinjaScript draft) on the index feeds | **RC1 minus $641; the two policy changes touch 21 of 342 sessions and carry none of the edge; every RC1 qualification stands** | same simulator, same 1.05M one-minute NQ bars, RC1 reproduced to the trade (342 / $11,661 / +0.1620 R), alpha.2 342 / $11,020 / +0.1551 R, H5 one-minute observation -$199 and H2 one-contract cap -$441, additive; matched control excess +0.094 R p 0.006; top 5% of trades 121% of net; 15:30 exit $14,208 of $11,020; 2023 flat; 15m CFD feeds cannot run a one-minute-defined rule | `docs/ib/STUDY_FTM_ALPHA2.md` |
| 2026-09-02 | Walk-forward, parameter, cluster, robustness and Monte Carlo tests on FTM 1.8.0-alpha.2; reverse-engineer its edge; start a durable library of mechanisms with real edge | **the edge is the breakout side (+0.10 R over a coin flip) and the first-signal timing; the stop is removable, the target should be, the direction model and every other component are inert; 'always long' beats it in 2025** | 14 component switches over 1.05M 1-minute bars with a random-entry control per variant; 200-cell grid 100% positive, 2 clusters / 5 components, IS->OOS Spearman -0.05; walk-forward best +0.197 vs default +0.170 OOS by choosing no target; +-20% max 0.07 R; costs survive 4x; DD at the 47th MC percentile; 60-day evaluation P(pass) 19.9% / bust 13.5% / neither 66.6% | `docs/ib/STUDY_FTM_ANATOMY.md`, `docs/ib/EDGE_LIBRARY.md` |
| 2026-09-02 | Design a brand-new intraday strategy from the edge library aiming at PF >= 1.5 and 66% win rate; then a trend-following design | **neither reaches the ask, the arithmetic needs +12 points of win rate and the honest lifts are +1 to +5; the trend design ships with evidence on US100, the limit design is marginal on NQ and null elsewhere; E1 corrected to a null at real costs** | every-bar limit 81%/PF 1.9 was two engine artifacts (fill-minute target, several live orders); corrected every-bar PF 0.81-1.02 on 256 geometries; MRL locked 60.8%/1.17 p 0.020/0.077; TFI NQ research 1.40 p 0.013, locked 1.24 p 0.33, US100 1.34/1.56/1.20, US30 null; gate is the component | `docs/ib/STUDY_NEW_DESIGN.md`, `pine/tfi/TFI_NQ_strategy.pine` |
| 2026-09-02 | Improve the TFI trend design's profit factor and win rate for XAUUSD | **re-selecting on gold gives 63% / PF 1.50 on research and PF 0.91 on 2025-26; the top cell 1.75 -> 0.75; the unchanged NQ defaults are 0.89 -> 1.20, the wrong shape; no gold settings recommended** | 24,192 cells, one marginal axis (08:30-11:30 window), locked read once with random-bar controls, eight neighbours all negative | `docs/ib/STUDY_NEW_DESIGN.md` §6, `research/mrl/tf_gold.py` |
| 2026-09-02 | Make the trend design more balanced with PF 1.50 on locked and a control pass; a US30 version | **the two-feed consensus is the shipped cell; nothing may be selected on locked; NQ+US100 correlate 0.90 so a book adds nothing; US30 has no version with evidence** | second-pass grid by min of NQ/US100 research R; hold-overnight PF 1.72 US100 vs 1.22 NQ, not selected; the MEGA_144K US30 configuration at one unit is 1.05/0.86/0.98 on US30's blocks, 1.25 on ISO 2026 where no filter is 1.29 | `docs/ib/STUDY_NEW_DESIGN.md` §7 |
| 2026-09-02 | Forward, parameter, robustness, Monte Carlo and other tests on the ported APM session-VWAP strategy, to see whether it passes for live trading | **the direction call is real (coin-flip-side control p 0.05 / 0.001 NQ, 0.012 / 0.034 US100) and the entry is a cost (a random bar on the same day and side beats it everywhere; the fill has chased 4 of a 5-ATR day); random-entry control passes NQ research 0.054 and US100 research / validation, fails US100 test 0.230, null on US30; research P(mean<=0) 0.052 on 70 trades; walk-forward re-selection loses to the defaults; not live-ready, forward-test 40+ trades** | numba engine reproducing the Pine trade for trade; three matched controls; same-selectivity random filter (NQ p 0.007, US100 0.010); 20-row anatomy; 12,960 + 8,640-cell grids with corr(research, later) +0.52 NQ / +0.31 US100 / -0.34 US30; walk-forward on three feeds; trade and day-block bootstrap, permutation drawdown (p99 $2,400 per MNQ); 47 clusters / 26 PCs; funded evaluation with zero-filled sessions | `docs/ib/STUDY_APM_VWAP.md`, `research/apm/` |
| 2026-09-02 | Reverse-engineer the APM session-VWAP strategy for its edge, add it to the edge library, and do feature engineering for it | **E11 added: a sustained >= 3 ATR displacement from a pre-market-anchored average, taken while the session VWAP is within 2.5 ATR of price, continues to the cash close; the plain opening drive and the published first-half-hour momentum are null on the same days; feature engineering ships nothing** | coin-flip-side null on the rule's own bars; a 0.5-5.0 ATR drive ladder and three fixed-bar momentum forms, all null or inconsistent; the two-half decomposition (band off +13.3 / +3.6, smoothing off +9.3 / +4.9, both off +0.3 / +1.6 against +24.0 / +10.8); 17 causal features x 2 halves x 2 feeds against a same-size random filter, two-feed agreement gate, one later-block read (fails: US100 test p 0.872) | `docs/ib/STUDY_APM_VWAP.md` §10-11, `docs/ib/EDGE_LIBRARY.md` E11, `research/apm/apm_edge.py` |
| 2026-09-03 | Test the ZetaFX Raschke trend-day / untouched-EMA EA in and out of sample, Monte Carlo, robustness and regimes, on all markets | **Not live-ready: US30 null over nine years, US100 fails research and passes the two later blocks, only NQ (43 trades) behaves correctly; pooled +0.104% of price at P(mean<=0) 0.0054 with 9% of trades carrying -93% of net** | 1m-vs-15m parity (corr 0.9975, the 15m fill 1.8 pts worse); day and mirrored-side matched controls on every block; 28-row anatomy; 168-cell grid; rolling walk-forward (re-selection loses to the shipped constants on all three feeds); trade and permutation Monte Carlo; volatility / trend / gap / year / weekday regimes; funded-evaluation P(pass); pooled cross-market in percent of price | `docs/ib/STUDY_TRENDDAY_EMA.md`, `research/trendday/`, `results/trendday/` |
| 2026-09-03 | Sweep 100k+ combinations of the trend-day family for a version with 5x the entries at PF ~2.0 that works on every market | **0 of 127,008 cells reach 5x entries at PF 2.0 on the RESEARCH block of all three long feeds, and 0 at PF 1.3; the frontier is monotone (1.92 at 1x, 1.70 at 2x, 1.28 at 5x) and the top 1,000 cells trade 0.42x the shipped rule** | two-phase sweep caching the day filter per (EMA, bucket); selection on research by the WORST of three feeds; entry-multiple frontier; top-1000 consensus; coherence gate over every axis neighbour (0 cells at 2x above 1.30); one reserved read with the multiplicity stated; matched day controls, day-block bootstrap and vectorbt portfolio stats on the 2x finalist | `docs/ib/STUDY_TRENDDAY_EMA.md` section 12, `research/trendday/td_sweep.py`, `results/trendday/sweep_analysis.txt` |
| 2026-09-03 | Add a Donchian channel to the trend-day family at the 3x / 5x / 8x entry rungs and sweep 100k+ combinations for a higher profit factor | **It does not raise any rung: +0.07 / +0.02 / +0.01 from a search 129x larger; the gate and midpoint target are negative everywhere and the only helpful 'stop' never fires (0% of exits) because every finalist places it beyond the channel, where it is an entry filter** | 543,948 cells a market on four feeds; Donchian as gate, stop and midpoint target; stops tested both inside and beyond the channel; per-use decomposition; coherence over every axis neighbour (worse than without: 1.07 vs 1.18 at 3x); top-1000 consensus (median entry multiple 0.05x); one reserved read; vectorbt ladder showing Sharpe 0.97 -> 0.71 -> 0.50 -> 0.42 as entries rise | `docs/ib/STUDY_TRENDDAY_EMA.md` section 13, `research/trendday/td_sweep2.py`, `results/trendday/dc_analysis.txt` |
| 2026-09-03 | Test the RTH VWAP Drift EVO 1 Sierra Chart study | **No edge: the direction call is real (beats a coin-flip side at p 0.000 on every 15m block, inverting loses 0.2-0.4 R) but pooled over 4,477 trades it is +0.079 R gross and -0.010 R net, P(mean<=0) 0.69; its ER filter is inert at 15m and harmful at 1m, and its recorded entry is a price the signal cannot reach** | both fill models diffed; coin-flip-side and inversion controls per block; 27-row anatomy; 2,880-cell grid per feed with rank transfer; trade bootstrap; cost stress; ER pass-rate measured at 1/5/15 minutes; regimes | `docs/ib/STUDY_VWAP_DRIFT.md`, `research/vwapdrift/`, `results/vwapdrift/` |
| 2026-09-03 | Rank every shipped strategy on this branch by profitability and put the top five through in-sample / out-of-sample, Monte Carlo, robustness and a live-trading test | **All five are profitable and none is separable from its own matched null: every one passes the parameter neighbourhood (73-100%), no strategy with more than one reserved block clears its control on a majority of them (IBS 3/7, APM 1/5, TFI 1/7, FTM 0/1, V56 1/1), and the day-block bootstrap clears 1/7, 2/5, 1/7, 0/1, 0/1** | eight engines put into one unit -- percent of entry price, one unit, each feed's own costs -- and ranked on RESEARCH only: IBS +9.91 %/yr, V56 CVD +7.94, FTM ORB +6.38, APM +2.44, TFI +2.43, trend-day +1.02, VWAP drift +0.85, CMMA +0.63. Three of five GREW out of sample (APM on all three feeds, one of which loses on research); all five survive 2x the assumed cost, which is the first time on this branch that execution is not the binding objection; at 2x notional a 60-day 8%/6% evaluation is 57-87% P(NEITHER) on four of five | **a control computed over ALL TRADES is a research-block statistic**: FTM's published +0.1013 R at p 0.004 reproduces over all 342 trades (p 0.005) and reads p 0.152 on the 147 locked trades alone. `ftm_sim` books commission in dollars and slippage in points, so its `pts` column is gross of the round turn (worth 0.62 %/yr here). What would move any of this is more RESERVED BLOCKS, not more strategies |
| 2026-09-03 | Optimise the V56 CVD exhausted-sellers rule to maximum performance: 2,177,280 nominal / 725,760 effective cells over timeframe, both channels, stop, target, pivot, window, the gate on/off, and four filters that survived elsewhere on this branch | **Nothing beats the incumbent. Nine declared finalists, none with a higher locked per-trade result, and the incumbent is the only one clearing its control there (p 0.012). 97.8% of the grid is profitable on research and corr(research, locked) is -0.026: the top 1% of research cells averages -0.0017 on locked against the whole population's +0.0508** | the two nulls split the answer -- the incumbent clears a same-selectivity random FILTER on locked and FAILS a random ENTRY (0.204), while the unfiltered 15m geometry does the reverse (p 0.002); no cell clears both. The gate ablation is the best evidence it has anywhere: 12 of 14 cells across two geometries and both blocks raise per-trade edge, and every one cuts total return because the gate removes 70-90% of signals | **score in percent of price, not R** -- the stop axis runs +0.347 to +0.175 in R and +7.5% to +9.1% in money, and the first ranking put a +2.33 R cell on top that returned +0.32%. The maximum-hold axis is INERT. The one real improvement is a change of geometry, not a tuning: 15-minute bars take 2.4x the trades for 48% more locked total return |
| 2026-09-03 | Remove ADX and CHOP from V61 and try the Money Flow Index and the momentum of an EMA cross as a confirmation entry | **Both null. Matched pairs improved on the locked block: MFI 49.3% where chance is 50%, EMA 58.0% with its ordering INVERTED at Spearman -0.618. The drop-one at the best cell of 2,055,874 says removing BOTH confirmations earns more in total (12.5% against 10.1% of price) with a better random-entry p (0.061 against 0.070)** | the base-rate table predicted it before any backtest: MFI>=50 passes 91.7% of breakout bars and EMA spread-rising 91.1%, so most readings of either are the trigger restated. Only the overbought CEILING (58.6%, lift 0.67) and the cross RECENCY form (14.9%) bind at all. Removing ADX and CHOP cost nothing | dropping the CVD gate at the same cell takes PF 1.68 to 1.15 and the entry null stops rejecting, so the gate is the strategy. Top 100 research cells read -0.0300 on locked against the population's +0.0461. Ships both readings default OFF with their locked shares in the tooltips |
| 2026-09-03 | Design a profitable trend-following strategy on a VWAP, a triple EMA cross and ATR | **7 of 8 blocks positive across three markets, clearing a random entry with identical geometry on 4 of the 7 blocks that had no part in the search** -- 30m long only, EMA 13>34>89 aligned within 30 bars, close above a rising session VWAP, ATR >= its 50-bar mean, 1.5N stop, no trail, no target, 480-bar cap. US100 +0.2200/+0.1596/+0.3842, US30 +0.1799/+0.1352/+0.1797, NQ +0.3585 research and -0.0401 locked | searched on US100 research only (146,880 cells, 87.4% profitable) then frozen; the volume weighting in VWAP is worth +0.0096 Sharpe over 69,003 matched pairs, so a session average price does the same job; removing the chandelier trail was worth 3.6x the per-trade result | it wins 9-19% of the time, 86% of trades stop out, the capped 14% supply 261-264% of net, the longest out-of-sample losing run is 28, and the median winner holds ten trading days. The maximum hold is LOAD-BEARING here where V61/V62 measured it inert. The block that chose the cell fails its own controls |
| 2026-09-03 | Use ATR as a regime filter on the V63 trend design and test it: 86 declared readings in four families, both directions, on eight blocks of three markets | **The expansion side works and the calm side does not: every floor/rising family has a positive mean edge (+0.006 to +0.047) and beats the no-regime baseline 57-71% of the time, every ceiling/falling family is negative (-0.028 to -0.054) at 34-48%. Three readings improve on 7 of 7 blocks that chose nothing** | it INVERTS V28, whose only ATR survivor was the bottom fifth of ATR's own 500-bar range -- re-run here that improves 3 of 7 blocks at -0.0615 and clears its control once. `atr/sma100>=1.0` is 7/7 at +0.0845 against the shipped sma50's 5/7 at +0.0693, same selectivity | 21.9% of 602 cells clear their control against a 5% chance rate while only 49.3% beat the no-regime baseline -- both true because the directions cancel, so read the direction split and never the pooled share. Every reading is NEGATIVE on the block that chose the strategy. The same gate read 7/7 in the drop-one and 5/7 here with only the base changed |
| 2026-09-03 | Add a time start/stop and a hard flatten to the V63 trend design, then run IS/OOS, Monte Carlo, robustness and a walk-forward optimisation | **The flatten costs -0.1710 %/trade averaged over seven windows -- 86% of the edge -- and four of the seven flattened windows are negative; no entry window beats all hours; and a walk-forward optimiser free to choose the flatten in 36 fold decisions chose it in 0 of 36** | mechanism plain: the median winner holds 240 hours and exits on the 480-bar cap, so a daily flatten truncates every winner and closes 55-72% of trades on the clock. Walk-forward efficiency against the fixed constants: 1.38/1.28 US100, 0.71/0.60 US30, 0.95/1.01 NQ -- mean 0.99, and the fixed constants win on fold consistency 5-6/6 against 3-6/6 | the flatten does cut pooled drawdown from 16.5% to 4.3-5.6%, and the permutation puts those realised drawdowns at the 2nd-6th percentile of their own distributions with a p99 of 13-15%, so most of the comfort is luck. 09:30-12:00 with no flatten is 7/7 blocks positive at +0.1964 against all hours' +0.1988 on 6/7 -- more consistent on half the trades, which is a preference and not an improvement |
| 2026-09-03 | Optimise the stop loss and take profit on the V63 trend design: 405 cells over 9 stops, 15 targets in both parameterisations, and 3 partial exits | **Nothing changes: 1.5 x ATR and no target are already the best rungs on the only unit that measures money adjusted for the path. The stop axis gives three different answers in three units -- monotone wider per trade, peaking at 2.5N in R, and FLAT in total money at one unit while drawdown climbs monotonically 16.5 to 38.2** | return-over-drawdown picks the tightest rung (1.5N at 10.3 against 8.0/7.9/6.6/3.5), and the per-trade column is a trade-count artifact: 434 trades at 6N against 850 at 1.5N for the same total money. This corrects two earlier per-trade readings in the same study that preferred 2.5N | no take profit wins monotonically in both parameterisations (16th time), and every target clears its own break-even win rate while still losing to no target, with the shortfall growing from +1.9% at 0.5R to +19.4% at 8R. The stop earns its place: at 12N, where it cannot bind, total falls to +133.9 and bootstrap P(mean<=0) goes 0.0003 to 0.0137. The 0.75N corner has a 5.5% ambiguous share against 0.0% for every wide cell |
| 2026-09-03 | Walk-forward optimisation of the Saty-phase configuration as given (21, 21, 4, +/-100, 09:30-10:30, opposing extreme, 61.8, 09:30-16:00, 2.5) | **The parameters are not worth optimising: the re-optimiser loses in 6 of 6 market-mode cells at mean WFE 0.17, and the given constants win on fold consistency too (7/7 and 11/13 against 5/7 and 7-9/13)** | all six axes re-chosen inside every training window from a 2,304-cell grid centred on the given values, rolling and expanding, three markets. The optimiser's per-fold choices agree with its own first fold only 38-52% of the time and it keeps the given value in vwap 0/7 and ema 1/7 on NQ | report WFE only against a POSITIVE baseline -- US30's is -0.0195 and the ratio printed -24 million before the guard. The configuration is two-market: US100 PF 1.90-2.53 on three blocks and NQ positive on both, against US30 negative on two of three. The 61.8 zone is drawn and never traded on, and two of the twelve given numbers have no field to map to |
| 2026-09-03 | Deep analysis: which indicators a profitable trend-following SCALP needs -- 31 declared conditions x 2 triggers x 2 geometries x 6 feed-timeframes | **The geometry flips the sign before any indicator: the same triggers earn -0.0033/-0.0052 %/trade at scalp geometry and +0.0948/+0.1191 at swing. Every indicator family is worth roughly a TENTH as much at scalp geometry, and the clock is the only family that does not shrink** | the zero-cost variant says it is not execution: gross +0.0039/+0.0023 against a cost of +0.0073/+0.0085, and the scalp win rate is 34.3%/33.6% against a driftless 2R bound of 33.3%. Cost as a fraction of risk runs 24.4% (NQ 5m, 0.75N) to 1.8% (US100 60m, 2.5N) | two conditions invert mechanically: ADX>=25 is 2nd-best at swing and negative at scalp; volume >= 1.5x its time-of-day mean is the best scalp condition (+0.0093) and the worst swing one (-0.0549). Fifth measurement that popular confirmations are the trigger restated: close>EMA50 passes 93.7% of signals, MACD>0 93.2%, ROC>0 91.0% |
| 2026-09-03 | `fast-alpha-overlay` on the Donchian trend follower: a 1-minute `(close-EMA20)/ATR20` reversion signal scheduling the entries of a 30m Donchian 20 / 2.5N breakout on NQ, four screening gates then the seven-part battery, run BOTH without and with a position lock | **DISBELIEVE the overlay -- but the position lock decides which objection you see.** Unlocked (5,045 overlapping trades) it reads +436.8 pts on +107,608 (+0.41%) with placebo percentile 87.5 (p 0.125), bootstrap p 0.706/0.357, haircut ratio 0.11, and 62.4% of the gain from DROPPED TRADES (n=4, all losers). Locked (227 trades, the tradeable book) it inverts: **Δ +508.5 (+3.61%), 100.0% entry price, 0 dropped, placebo p 0.000, bootstrap p 0.083/0.099, haircut ratio 2.98, K sweep plateaus**. **The block split kills it either way: research +0.586 pts/trade at placebo p 0.125 with a median of exactly +0.000, locked +5.526 at p 0.000** -- absent where it was chosen, present where it should decay, the wrong shape for the eighth time | the size names the mechanism: mean entry improvement **+1.162 bps against half the Roll implied effective spread of 0.1871 -- 6.2x**, which no waiting can capture. It is a better LEVEL after a median 3-minute pullback, not a better fill -- a SIGNAL change with a signal's selection burden, the sixth route to `STUDY_LIMIT_ENTRY` / `research/atme/`. Top 5% of trades (11 of 227) supply **101.1%** of the gain; 216 trades lose in aggregate; cost cancels exactly (Δ +508 at 1x/2x/4x/8x) because the counts are identical | two screening gates fail before anything is built: fast drift at signal bars -0.0422 bps against a slow accrual of +1.4880 bps/min (**0.03x**), and the direct fast edge +0.0304 bps is **0.16x** half the Roll spread, so the reversion is substantially bid-ask bounce. **Report the baseline's CONCURRENCY before reporting an overlay's attribution** -- an unlocked baseline counts the same move twenty times and turns a price effect into a population effect. Confirms `STUDY_V50_SELECTION` from the execution side: the adverse open gap on continuous futures is +0.0000 ATR, so there is no implementation shortfall for a scheduler to recover |
| 2026-09-03 | ORB v1 to spec: a one-trade-per-session opening-range breakout on NQ (5m bars, 09:30-09:45 range, HTF EMA20/50, session VWAP, ATR(14) frozen at the signal, 0.25% equity risk, 1xATR stop, 50% at 1R then breakeven then 2R), three chronological blocks, 4,320-cell sensitivity | **No demonstrable edge, and the reason is the gate rather than the rule.** `range_size / ATR` compares a 15-MINUTE range to an ATR on an UNSTATED timeframe; under the literal reading the median ratio is **2.45**, so the specified [0.3, 1.5] band keeps the quietest **11.1%** of sessions and the rule fires **31 times in 765**. Development **-$88.31/trade PF 0.367 Sharpe -1.35**, validation +$127.60 PF 4.15, out-of-sample +$25.39 PF 1.250 -- **loses on the only block allowed to choose**, the wrong shape for the ninth time | clears NO control: random post-range bar with the same session/side/geometry **p 0.478**, coin-flip side **p 0.516**, bootstrap P(mean<=0) **0.629** whole-sample and 0.379 OOS; it does beat always-long (-$28.29 vs -$13.14). 4,320 cells: **only 24.2% profitable in-sample**, corr(IS,OOS) +0.186, every axis marginal negative except a 240m ATR which wins by making the gate inert (95.3% pass). The marginal consensus goes +$6.22 IS to **-$26.50 OOS with a 9-trade losing streak** -- seventh re-optimiser here to lose to its starting point | **the ATR timeframe moves the pass rate 1.4% -> 95.3%** and is the dominant unstated parameter. **Costs are NOT binding, which is rare on this branch**: 2x slippage costs $0.80-$1.83/trade because a 1xATR stop is ~6% of risk against a scalp's 24%. The intrabar tie-break was ANSWERED not assumed -- exits walked on the true 1-minute path leave **0.00%** ambiguous and flipping the assumption is worth **$0**. At 0.25% risk on $100k with MNQ's $2 point value the median size is 3 lots, so **12.9% of trades cannot scale out at all** -- the "exit 50%" instruction rounds to zero. Single-market: US100/US30 are 15m here and carry neither a 15m opening range nor a 1m exit path |
| 2026-09-03 | ORB v1 frozen and run on three more feeds (US100 15m, US30 15m, US30_ISO 15m -- all three verified byte-identical / row-and-span-identical against the registry), then a market-regime filter added: ADX(14)/+DI/-DI, EMA20/50, normalised slope and hysteresis on completed 15-minute bars | **The gate was written for a 15-minute chart.** On 15m bars the opening range is ONE bar so `range_size / ATR` is ~1 by construction -- median 1.54-1.88 against **2.45 on 5m** -- and [0.3, 1.5] passes **30-47% of sessions instead of 11%**. Unfiltered across four feeds (724 trades, 10 blocks) **1 of 10 clears a random-entry control (US30 research p 0.007) and it is the block that would choose**; US30's own validation and test read p 0.983 / 0.973 and an independent second provider reads **p 0.990** | **The regime filter's CHOP exclusion is the one thing that agrees 4 of 4**: the trades it removes lose money on every feed (-$5.15 / -$15.33 / -$9.75 / -$64.79, PF 0.49-0.95) while removing 54-58% of the sample. But **two of four reserved blocks improve and two do not**, the worst deterioration (US30 test PF 0.537 -> 0.332) is on the market whose research block looked best, and only US100's test crosses zero (PF 1.047, n 26). It is loss avoidance, not an edge | **THE HYSTERESIS IS INERT** -- entry=exit=25 reclassifies **0.8-0.9% of bars** and the adx_exit axis is flat TO THE CENT at 15/18/20, because ADX(14) on 15m rarely lingers in the 20-25 band. **324-cell threshold sweep: corr(IS, OOS) -0.633 Pearson / -0.656 Spearman**, so tuning is worse than not tuning; share profitable is exactly 66.7% at EVERY setting of EVERY axis because the **spread across markets (-48 to +44) is an order of magnitude larger than the spread across thresholds within a market (<=10)**. BEAR is the worst bucket on two of four feeds. Costs are 3.7-5.4% of a 1xATR stop and 2x slippage flips no sign on any of ten blocks |
| 2026-09-04 | Optuna (TPE + NSGA-II) on the V61 CVD rule: 6,000 trials over a CONTINUOUS space the exhaustive grid could not reach, three objectives, fANOVA importance, a widened box, the V30 surrogate test | **Nothing beaten.** Research total climbed **+18.88% -> +40.93% -> +55.40%** as the search got harder and wider while locked total went **+12.14% -> +8.62% -> +16.34%** against the shipped 15m preset's **+17.91%**; research Sharpe rose monotonically 1.37 -> 2.81 and locked Sharpe did not follow (1.19, 1.71, 1.70, 1.03, 0.87). None of six finalists beat the shipped presets out of sample | **The median-of-8-folds objective transferred WORST of the three** -- research +26.43% -> locked **-0.07%**, the only losing finalist, on a 63-trade cell. **Widen the box and the optimum runs to the new ceiling** (stop 7.61 of 8.0, target 10.35 of 12.0) = a stop that cannot bind and a target never reached, the no-take-profit finding through the back door for the 16th time | **fANOVA is the one durable output**: timeframe 0.477 and CVD window w 0.209 carry it; pivot k 0.006, prior-session-high 0.003, adaptive stop 0.005, max hold 0.012 are noise. V30's surrogate reproduces: random-row R^2 +0.8956 vs **-6.92** holding out a whole timeframe. **A POSITIVE TRANSFER CORRELATION CAN BE THE SAMPLER**: +0.32 Pearson over 2,496 configs against the grid's -0.026, because TPE concentrates in a narrow region (99.1% profitable on research) so the range is restricted -- it does not mean research ranking picks winners. A sampler cannot beat an exhaustive search on the same space; run one only for the continuum, a different objective, or interaction-aware importance |
| 2026-09-04 | Walk-forward on the V61 CVD rule with the selection RE-RUN INSIDE every training window: 19,200 declared cells, 9 quarterly test folds, rolling 4Q and expanding, five arms including a RANDOM cell from the same grid | **Over all nine folds the shipped 15m preset wins both schemes** -- +34.27% against a re-chosen +28.20% (rolling) and +24.50% (expanding), random grid cell +8.28% -- and it is the only arm with **no losing quarter** (9/9, worst fold +1.70). **But the fixed arms had seen five of nine folds** (research block ends 2024-11-27), so on the four post-cut quarters the schemes disagree: rolling FIXED15 +17.07 vs +14.23, **expanding re-chosen +19.84 vs +17.07** -- the first honest slice on this branch where a re-optimiser leads, on four folds | **The distinction both schemes agree on**: selecting from this family beats picking from it ARBITRARILY (3/4 post-cut, 8/9 and 5/9 overall) while re-selecting EVERY FOLD does not beat NEVER selecting. **Normalise WFE by span or it is a span ratio** -- raw 0.145/0.094 becomes **0.582 rolling / 0.751 expanding** per quarter, and expanding has the higher efficiency with the lower absolute return | **The stability table is the best output**: entry channel 15 in **9/9** folds and exit 30 in 8/9 -- exactly the shipped 15m preset's channels and never the incumbent's 20/20 -- and NO TAKE PROFIT in **8/9**, chosen freshly in every training window (17th confirmation). Modal share on timeframe/stop/k/w is 44-78%: it never settles. **All three arms clear a geometry-matched random entry at p 0.000 on the walk-forward OOS span** (incumbent +0.1512 %/trade vs control median +0.0438), the strongest evidence the rule has. Entry 15 is the grid minimum, so that axis is on the box edge as in the Optuna study |
| 2026-09-04 | Monte Carlo PERTURBATION on the three V61 presets -- price jitter with every indicator recomputed from the jittered bars, execution noise inside the walk, missed fills, one-rung and joint parameter jitter -- beside a permutation for the path and a bootstrap for the edge | **The rule is not on a knife edge and its weak link is sample size.** OHLC jittered up to 2 ticks with ATR, both channels and the CVD pivot structure RECOMPUTED: **sign kept in 750 of 750 draws**, trade counts 85->87 / 209->211 / 166->166. Execution noise (slip U(0,2x), cost U(0.5x,2x)) gives a p5-p95 band half a point wide, P(<=0) 0.000. 40% missed fills leaves all three positive | **The parameter perturbation inverts the usual ranking**: the pre-declared INCUMBENT reads joint-jitter p5 **+0.71**, P(<=0) **0.036**, worst one-rung neighbour **+4.24** and only **41% of neighbours beat it** -- a narrow ridge -- while the 15m preset and Pareto read p5 +13.59 / +10.41, P(<=0) 0.000 and **75% / 70% of neighbours BEAT them**, which is what a cell not cherry-picked from a spike looks like. `hold` exactly inert again (3rd confirmation) | **The bootstrap is the weak link**: no preset's 95% CI cleanly excludes zero on the locked block and the incumbent's **P(mean<=0) is 0.110 on 85 trades**; the other two reach 0.027 / 0.026 on 208 and 166 trades with a SMALLER per-trade edge, i.e. through sample size. `STUDY_V15_BOOK`'s split reproduces: **p 0.000 against a matched random entry, 0.110 against zero**. The permutation says the incumbent's realised path was UNLUCKY -- drawdown at the **95th percentile** of reshuffles of its own trades -- and MC p99 drawdown is **1.19x / 1.73x / 1.84x** realised, the sizing number. A perturbation prices noise on the trades you SELECTED, never the selection |
| 2026-09-04 | A posted discretionary rule, the "IB 25 retracement": session VWAP anchored 09:30, nothing before the 10:20 close, fib on the 09:30-10:30 range, resting limit at 25% from the target-side extreme, target that extreme, stop at 50%, no entries past 12:00 or after a sweep -- transcribed to NQ 1-minute, one live order, one trade a session, then priced as MNQ | **Negative gross, and the win rate is its own break-even.** Research **-0.0169 %/trade, PF 0.807, -$6.55 an MNQ contract**; locked +0.0097 / PF 1.111 / +$4.42 -- losing where it would be selected, the wrong shape for the 10th time. Gross **-$0.11 a trade** before any cost, so fills cannot rescue it; it **loses to a random entry MINUTE at p 0.845** and research bootstrap P(mean<=0) is **0.965** | **The post's own best observation is correct and worthless**: the 75% stop raises the win rate 48.4% -> 66.4% exactly as claimed and expectancy stays negative, because win rate tracks the driftless break-even at every rung (32.5/28.6, 48.4/50.0, 56.5/60.0, 66.4/66.7, 74.2/75.0). **The two judgement calls earn nothing** -- 0 of 16 slope x chop cells positive, the chop ceiling removes 63% of the sample and moves the result by 0.0001. In MNQ terms: round turn **$3.44 = 6.5% of a $53 median risk**, one contract loses **~$1,050/yr** with a **$2,480** drawdown, and it is negative even at FEES ONLY | **Two sign errors caught by diagnostics, not by reading**: the control had target/stop swapped so every draw exited instantly in profit (median +0.1513, 5-95% band of ZERO width); and slippage was applied in the trader's FAVOUR at both entry and exit, worth $1.00 an MNQ trade, caught because expectancy ROSE with assumed slippage. **A dollar answer needs the synthetic-level deflator** -- stored NQ runs 1.2563 -> 1.0182 above the real index over 862 overlapping days, inflating research dollars **11.7%**; percent of price, R and win rates are unaffected. Only retr 0.50 / stop 0.75 clears its own break-even (36.0% vs 33.3%) at gross +$4.24 / net -$0.20 -- a pure cost problem |
| 2026-09-04 | The submitted "NQ Scalping System" Pine (EMA89 trend, EMA8/21 pullback, StochRSI reset/cross, ATR 1.5/2.5, fixed 15/8-point trail, 06:00-11:30 Chicago, 5 MNQ), transcribed with its order model, IS/OOS on NQ 1m/5m/15m and US100 15m, ablations, controls, 160-cell geometry sweep, 729-cell in-fold walk-forward, perturbation Monte Carlo | **No edge, and the configured exit machine loses on ANY entry.** NQ 5m research **PF 0.393 / -$92,951 at 5 MNQ**, locked 0.445 / -$67,843; exits 62% trail, 37% stop, **1% target**. Negative on 8 of 8 blocks across four feed-timeframes. Random-entry control band at the configured geometry is **entirely negative** [-0.0447, -0.0376]; trail off, a random bar beats the rule (p 0.730) | **The trail inverts the reward:risk by arithmetic**: median 5m ATR 10.4 pts, so a 15-pt arm sits at the 16-pt stop distance and 60% of the way to the 26-pt target -- 62% of trades exit at ~+7 vs -16. Trail OFF: PF 0.393 -> **0.863**; the code's own ATR trail is as bad (0.397). **The naked fill bar hid a third of the loss**: protecting it, PF 0.393 -> 0.151, -$92,951 -> -$118,465. Entry ablation: only the EMA89 gate helps; shorts do the damage (0.789 vs 0.942) in a market that rose 89% | **The entry has no reproducible information**: short-side fixed-horizon excess +0.13/+0.21 ATR at p 0.007/0.000 on NQ 5m research is **-0.03/-0.00 on locked (p 0.64/0.48)**, negative on NQ 15m, absent on US100. Geometry: **0 of 80 short cells net-profitable**; long positives are all no-target no-hold drift. Walk-forward: **a RANDOM cell beats both re-chosen and fixed** in both schemes (8th re-optimiser to lose, 1st to lose to random). MC trail-off: research P(total>0) **0.000** under execution and price jitter, bootstrap CI excludes zero on the NEGATIVE side. Shipped v2 with trail off, fill bar protected, isconfirmed guard -- mechanics fixed, no edge claimed |
| 2026-09-04 | The uploaded Diversified Trend Ensemble spec (five EWMAC sleeves with hard-coded scalars, FDM, blended vol, rolling IDM, vol target 0.20, no-trade buffer, t+1-open execution, NO optimisation) implemented to its own section 7 layout and run through its section 8 battery on the universe available here: US100 + US30 daily, 2016-11 to 2025-07 | **Every implementation test passes on first run and the strategy has no edge on N = 2 in one asset class -- which the spec itself predicts.** Alignment clean (0.023 / 0.015; same-bar execution would have manufactured Sharpe 1.13 / 0.82); E\|F\| 9.8-12.2 with the scalars as given; calibration c 0.7885 hits tau exactly; buffering cuts turnover to 7.9 / 9.4 turns/yr | training net Sharpe **-0.037**, deflated Sharpe **0.039** at N = 12, breakeven cost **negative**, random null percentile **47**, bootstrap P(Sharpe<0) **0.54**, perturbation surface a **flat plateau at zero**. The spec's 1260/252 walk-forward yields ONE fold on 6.5 years (-0.80); a labelled 756/252 supplement reads +0.88 / +0.52 / -0.80, sd 0.88. CPCV is degenerate on a parameter-free strategy (five identical paths). Holdout, once: **+0.54**, better than training, the wrong shape, failing the spec's own within-0.3 test | **2022 was -20.1% at Sharpe -1.15 while SHORT US100 on 83% of days as it fell 33.8%** -- whipsawed by bear rallies one asset class cannot escape; vol terciles low +0.96 / high -1.06, the opposite of crisis convexity. Two correlated indices run HOTTER than target (c < 1), not cooler as the spec's 20-instrument simulation. The design is untested because its engine -- breadth -- is absent; the registry's XAUUSD / EURUSD / BTC feeds would take N to 5 across three asset classes and are not on disk. Fourth name-shadowing bug this session: a skill directory must be inserted on sys.path AFTER the pipeline modules push `research/` to the front |
| 2026-09-04 | EMA 13/48 cross x VWAP as support/resistance x 1.5 ATR stop x ATR trailing stop, intraday NQ 5m, then 37 causal features and a deep-learning ladder (ridge/logistic, LightGBM, XGBoost, MLP 2x64, MLP 4x128) purged and embargoed with shuffled twins, on two bases | **No profitable intraday strategy, and the ask's own components prevent it.** 24-cell grid: trail ON averages **PF 0.201**, trail OFF 1.309; the random-entry band with the trail is entirely negative and the rule is worse than random inside it (p 0.937). Trail off, the intraday base is PF **1.02** and fails a random entry (p 0.163). Only no-trail NO-FLATTEN cells exceed PF 1.3, at 1-3% win rates -- drift | **Deep learning reproduces V28**: on the as-asked base capacity is monotonically harmful and logistic wins the ladder, finding at best a **-0.30 R / PF 0.32** subset of a PF-0.19 base; on the no-trail base 5 of 20 research cells clear a random filter (1 expected) and the pre-declared locked read **inverts** -- base +0.113 / PF 1.164, filtered 30% **-0.078 / PF 0.870** -- because a win/lose model discards the tail (p90 2.40 -> 1.85). Read p90 of R, not AUC | VWAP STATE gate is the one positive marginal (+0.03); TOUCH is the worst. Stop ladder flat and negative 1.0-3.0 ATR (V18 stands). Ridge's top coefficient on the no-trail base is RSI14 at **-0.78** -- the tenth route to mean reversion. Features: 0 leaks on 30 probes. Locked base better than research (1.164 vs 1.052), the wrong shape. Operational: two torch ladders on four cores oversubscribe and stall -- run them sequentially |
| 2026-09-05 | Correction to the row above, after the user confirmed the screenshot's chart is MNQ 15-minute with NO execution option ticked: re-read `strategy.exit`'s behaviour on the FILL bar itself, verified trade-for-trade against an independent Python reference | **The original transliteration left the fill bar naked and understated the result.** `strategy.exit` runs on the signal bar too, before the entry fills, with `stop=`/`limit=` still `na` -- only the trail's distances are known that early, so the fill bar carries a live TRAIL and no hard stop or target. Corrected: NQ 15m research PF **0.669 -> 1.739**, win 46.1% -> 70.3%, on the identical rule and costs | A 15-minute bar's median range (19.0 pts) exceeds the 15-point trail-arm distance, so the trail activates **inside the fill bar on ~62% of entries** -- a third of all trades (33.4%) exit there, 100% winners by construction, mean +31.8 pts. The rest win only 60.3% at -1.1 pts, close to a coin flip | **Still does not clear a matched control**: 15m research p 0.180, locked p 0.213; 5m passes only on the block that would select it (p 0.023 locked, 0.813 research -- the wrong shape). The shipped v2 script was never affected -- its fill-relative bracket is placed with the entry, both known at order time |
| 2026-09-05 | Three posted indicators combined as asked -- the 50% session level (kijunkun/milly) as support/resistance, MTF ICT swing highs/lows (1H/4H/D/W/M) as reversal levels, absorption bubbles (profitprotrading) as confirmation, 1.5 ATR stop and an ATR trailing stop. NQ 15m with true contract volume, US100/US30 cross-market, truncation audit clean (0 of 12 columns leaking) | **No edge; loses on both blocks and to a coin flip.** As specified: research 1,888 trades **-0.0070 %/trade, PF 0.920**; locked 1,032, **-0.0251, PF 0.778**. A random entry with the identical stop and trail beats it on **both** blocks (p 0.865 / 0.990). 211-cell grid 49.8% profitable, median -0.0003 | **The trail is the whole result and it works on any entry.** Tightening it is monotone: 0.25/0.25 ATR reads **PF 1.751 / 75.2% win / 1-bar hold**; a RANDOM entry with the same trail reads 1.617 / 73.7%, and on the locked block the random entry **WINS** (1.684 vs 1.787, p 0.610). **46% of that cell is the intrabar tie-break** -- stop-first instead of Pine's path takes +0.0237 to +0.0129. Sign OPPOSITE to STUDY_EMA48_VWAP_DL's destructive trail; same root cause | **Both filters subtract**: full rule +0.0034 / PF 1.061, minus absorption **+0.0096 / 1.181**, minus levels +0.0088 / 1.159, each on 2x the trades. The published absorption threshold passes **78.7%** of all bars (the A-E buckets only size the dot) -- but its lift on level-touch bars is a real **1.15-1.55x**, so unlike RSI/Aroon/MACD/MFI it is not the trigger restated, just not profitable. Long marginal +0.3130 vs short -0.1065 is drift (NQ rose 89%). Removing the trail is not the fix -- that ablation is degenerate (110 trades, 1.8% win) since the design has no other exit |
